name: Wire OMEGA V2
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  wire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Wire runtime assets into index
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          css='<link rel="stylesheet" href="omega_ui_interaction_fix_v2.css">'
          js='<script src="omega_minister_runtime_v2.js" defer></script>'
          if 'omega_ui_interaction_fix_v2.css' not in s:
              s=s.replace('</head>', '    '+css+'\n</head>', 1)
          if 'omega_minister_runtime_v2.js' not in s:
              s=s.replace('</body>', '    '+js+'\n</body>', 1)
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit wiring
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Wire OMEGA Minister Runtime V2 and UI interaction fix'
          git push
