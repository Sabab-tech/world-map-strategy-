/** MINISTER QUERY ROUTER v5.1.0 - semantic router + answer-provider UI */
(function(global){'use strict';
const VERSION='5.1.0';
const QueryIntent=Object.freeze({MINISTER_IDENTITY:'MINISTER_IDENTITY',RESOURCE_SECURITY:'RESOURCE_SECURITY',RESOURCE_STATUS:'RESOURCE_STATUS',RESOURCE_MINING_DISCOVERY:'RESOURCE_MINING_DISCOVERY',RESOURCE_PROCESSING_REFINING:'RESOURCE_PROCESSING_REFINING',RESOURCE_TRADE_TARIFF:'RESOURCE_TRADE_TARIFF',RESOURCE_LOGISTICS:'RESOURCE_LOGISTICS',RESOURCE_FORECAST:'RESOURCE_FORECAST',MACROECONOMICS:'MACROECONOMICS',DEMOGRAPHICS:'DEMOGRAPHICS',DEFENSE_MILITARY:'DEFENSE_MILITARY',ENERGY_POWER:'ENERGY_POWER',POLICY_DIRECTIVE:'POLICY_DIRECTIVE',GENERAL_STRATEGIC:'GENERAL_STRATEGIC',UNKNOWN:'UNKNOWN'});
function routeMinisterQuery(prompt,ministerContext={},worldContext={}){const b=global.OfflineSemanticBrain;if(!b)return{version:VERSION,intent:QueryIntent.UNKNOWN,domain:'UNKNOWN',entities:[],requiredData:[],confidence:0,error:'OFFLINE_SEMANTIC_BRAIN_NOT_INITIALIZED'};const p=b.parse(prompt,{countryId:worldContext.countryCode||ministerContext.countryCode,resourceId:worldContext.resourceId,ministerId:ministerContext.ministerId});const map={MINISTER_IDENTITY:QueryIntent.MINISTER_IDENTITY,TRADE:QueryIntent.RESOURCE_TRADE_TARIFF,RESOURCE:QueryIntent.RESOURCE_STATUS,CAUSE:QueryIntent.GENERAL_STRATEGIC,STRATEGIC:QueryIntent.POLICY_DIRECTIVE,GENERAL:QueryIntent.GENERAL_STRATEGIC};const intent=map[p.intent]||p.intent||QueryIntent.UNKNOWN;const entities=[];for(const[type,e]of Object.entries(p.entities||{}))if(e?.id)entities.push({type,id:e.id,confidence:e.confidence,source:e.source,surface:e.surface});const requiredData=[...(p.sourcePlan||[])];if(p.operation==='READ_PROPERTY')requiredData.push('MINISTER_PROFILE');if(p.operation==='TRADE_SOURCE')requiredData.push('SUPPLIER_RANKING');return{version:VERSION,intent,domain:p.intent||'GENERAL',entities,requiredData:[...new Set(requiredData)],confidence:p.confidence,semantic:p,executable:p.executable};}
function el(id){return typeof document!=='undefined'?document.getElementById(id):null}
function ensureAnswerPanel(){if(typeof document==='undefined')return null;let n=el('omega-ai-answer');if(!n){const history=el('interrogation-chat-history');if(!history)return null;n=document.createElement('div');n.id='omega-ai-answer';n.style.cssText='margin:8px 0;padding:10px 12px;border:1px solid rgba(0,229,255,.28);border-radius:8px;line-height:1.45;white-space:pre-wrap;';history.appendChild(n)}if(!el('omega-answer-provider')){const head=document.createElement('div');head.id='omega-answer-header';head.style.cssText='display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:7px;padding-bottom:6px;border-bottom:1px solid rgba(0,229,255,.16);';const label=document.createElement('span');label.textContent='ANSWER PROVIDER';label.style.cssText='font:11px monospace;opacity:.72;';const box=document.createElement('select');box.id='omega-answer-provider';box.setAttribute('aria-label','Answer provider');box.style.cssText='min-width:120px;height:32px;padding:0 8px;border:1px solid rgba(0,229,255,.45);border-radius:6px;background:#07111b;color:#dff8ff;font:12px monospace;';box.innerHTML='<option value="offline">OFFLINE AI</option><option value="google">GOOGLE AI</option>';let saved='offline';try{saved=localStorage.getItem('omega.answerProvider')||'offline'}catch(_){}box.value=saved;box.addEventListener('change',()=>{try{localStorage.setItem('omega.answerProvider',box.value)}catch(_){}});head.append(label,box);n.prepend(head)}return n}
function contextFromPage(){const root=document.querySelector('[data-country-code]');return{countryCode:root?.getAttribute('data-country-code')||global.OmegaGameState?.countryCode||global.GameState?.countryCode||'',countryName:root?.getAttribute('data-country-name')||global.OmegaGameState?.countryName||'',ministerId:global.OmegaCabinetUI?.currentMinister?.id||global.OmegaCabinetUI?.currentMinisterId||'',ministryId:global.OmegaCabinetUI?.currentMinistryId||''}}
async function submitProviderQuery(){const input=el('interrogation-input'),panel=ensureAnswerPanel(),provider=el('omega-answer-provider');if(!input||!input.value.trim())return;const prompt=input.value.trim(),ctx=contextFromPage(),mode=provider?.value||'offline';if(panel)panel.lastChild.textContent='';try{let data;if(mode==='offline'){const r=await fetch('/api/ai/semantic-query',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,countryId:ctx.countryCode,ministerId:ctx.ministerId})});data=await r.json();if(!r.ok||!data.ok)throw new Error(data.error||'Offline query failed');panel.lastChild.textContent=data.result?.text||data.result?.answer||'The offline engine returned no answer.'}else{const r=await fetch('/api/ai/minister-consult',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,ministerId:ctx.ministerId,ministerName:global.OmegaCabinetUI?.currentMinister?.name||'',ministryId:ctx.ministryId,countryName:ctx.countryName,countryCode:ctx.countryCode,language:/[\u0980-\u09FF]/.test(prompt)?'bn':'en'})});data=await r.json();if(!r.ok||!data.ok)throw new Error(data.error||'Google query failed');panel.lastChild.textContent=data.text||data.result?.text||'Google AI returned no answer.'}}catch(err){if(panel)panel.lastChild.textContent=`Answer error: ${err.message||err}`}}
function install(){const ready=()=>{ensureAnswerPanel();const b=el('btn-submit-interrogation');if(!b||b.__omegaProviderBound)return;b.__omegaProviderBound=true;b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();submitProviderQuery()},true)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ready,{once:true});else ready();setTimeout(ready,500);setTimeout(ready,1500)}
if(typeof document!=='undefined')install();
const api=Object.freeze({VERSION,QueryIntent,routeMinisterQuery,install,submitProviderQuery});global.MinisterQueryRouter=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;
})(typeof globalThis!=='undefined'?globalThis:window);
