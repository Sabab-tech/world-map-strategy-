(function(global){'use strict';
const VERSION='2.1.0',LEARNED_KEY='OMEGA_OFFLINE_SEMANTIC_LEARNING_V2';let runtime={countries:[],resources:[],vocabulary:null,datasets:[]};
const text=v=>String(v==null?'':v).trim(),norm=v=>text(v).normalize('NFKC').toLowerCase().replace(/[?!,.:;'"(){}\[\]]/g,' ').replace(/\s+/g,' ').trim(),isBn=v=>/[\u0980-\u09FF]/.test(text(v)),object=v=>v&&typeof v==='object';
function storage(){try{return global.localStorage||null}catch(_){return null}}function learned(){const s=storage();if(!s)return{};try{return JSON.parse(s.getItem(LEARNED_KEY)||'{}')}catch(_){return{}}}function saveLearned(v){const s=storage();if(!s)return false;try{s.setItem(LEARNED_KEY,JSON.stringify(v));return true}catch(_){return false}}function learn(surface,type,id,confidence=1){if(!surface||!type||!id||confidence<.9)return false;const m=learned();m[`${type}:${norm(surface)}`]={type,id,confidence,updatedAt:Date.now()};const k=Object.keys(m);while(k.length>512)delete m[k.shift()];return saveLearned(m)}
function idOf(v,keyHint=''){if(!object(v))return text(keyHint).toUpperCase()||null;const c=[v.id,v.iso,v.iso3,v.isoCode,v.countryCode,v.code,v.canonical_id,v.canonicalId,keyHint],x=c.find(y=>/^[A-Za-z][A-Za-z0-9_-]{1,31}$/.test(text(y)));return x?text(x).toUpperCase():null}
function namesOf(v,keyHint=''){if(!object(v))return keyHint?[keyHint]:[];const keys=['name','countryName','officialName','shortName','displayName','label','title','resourceName','canonicalName','localName','nativeName','name_en','name_bn','identity'],out=[];for(const k of keys){const x=v[k];if(typeof x==='string'&&x.trim())out.push(x);else if(object(x))for(const y of Object.values(x))if(typeof y==='string'&&y.trim())out.push(y)}if(keyHint)out.push(keyHint.replace(/[_-]+/g,' '));return [...new Set(out.map(text).filter(Boolean))]}
function collectRuntimeEntities(datasets){const countries=new Map(),resources=new Map(),add=(map,id,names,meta)=>{if(!id)return;const e=map.get(id)||{id,names:new Set(),meta:meta||null};names.forEach(n=>e.names.add(n));map.set(id,e)},visit=(node,keyHint='')=>{if(!object(node))return;const key=norm(keyHint),id=idOf(node,keyHint),names=namesOf(node,keyHint),country=!!id&&(id.length===3||/country|sovereign|nation|state|countryprofile/.test(key)),resource=!!id&&/resource|commodity|mineral|hydrocarbon|fuel|ore|metal/.test(key);if(country)add(countries,id,names,node);if(resource)add(resources,id,names,node);for(const[k,x]of Object.entries(node))if(object(x))visit(x,k)};datasets.forEach(d=>visit(d));return{countries:[...countries.values()].map(e=>({...e,names:[...e.names]})),resources:[...resources.values()].map(e=>({...e,names:[...e.names]}))}}
function configure(options={}){const datasets=Array.isArray(options.datasets)?options.datasets.filter(object):[],e=collectRuntimeEntities(datasets);runtime={countries:e.countries,resources:e.resources,vocabulary:options.vocabulary||null,datasets};return{version:VERSION,countries:runtime.countries.length,resources:runtime.resources.length}}
function vocabularyFor(lang){const v=runtime.vocabulary||{};return v.languages?.[lang]||v.languages?.en||{}}
function matchPhrases(q,groups){const hits=[];if(!object(groups))return hits;for(const[s,aliases]of Object.entries(groups))for(const alias of Array.isArray(aliases)?aliases:[]){const a=norm(alias);if(a&&q.includes(a))hits.push({semantic:s,alias,score:a.length})}return hits.sort((a,b)=>b.score-a.score)}
function detectQuestion(q,lang){const h=matchPhrases(q,vocabularyFor(lang).question||{});if(!h.length)return{type:'UNKNOWN',confidence:0};const m={what:'IDENTIFY',who:'PERSON',which:'SELECT',where:'LOCATION',when:'TIME',why:'CAUSE',how:'METHOD',how_many:'COUNT',how_much:'QUANTITY',should:'POLICY',can:'FEASIBILITY',will:'FORECAST'},b=h[0];return{type:m[b.semantic]||b.semantic,phrase:b.alias,confidence:Math.min(.99,.6+b.score/100)}}
function detectOperator(q,lang){return matchPhrases(q,vocabularyFor(lang).operators||{})[0]?.semantic||null}
function resolveEntity(q,entities,type){const hits=[];for(const e of entities)for(const n of e.names){const a=norm(n);if(!a)continue;if(q===a)hits.push({e,n,score:1});else if(q.includes(a))hits.push({e,n,score:Math.min(.98,.65+a.length/Math.max(100,q.length*3))})}for(const[k,v]of Object.entries(learned()))if(v.type===type&&q.includes(k.slice(type.length+1)))hits.push({e:{id:v.id,names:[k.slice(type.length+1)]},n:k.slice(type.length+1),score:v.confidence});hits.sort((a,b)=>b.score-a.score||b.n.length-a.n.length);if(!hits.length)return{id:null,type,confidence:0,source:'UNRESOLVED'};const best=hits[0],second=hits[1]?.score||0;if(best.score<.8||best.score-second<.06)return{id:null,type,confidence:best.score,source:'AMBIGUOUS',candidates:hits.slice(0,5).map(x=>({id:x.e.id,surface:x.n,confidence:x.score}))};return{id:best.e.id,type,confidence:best.score,source:'RUNTIME_DATASET',surface:best.n}}
function inferAsset(q,lang){const groups=vocabularyFor(lang).assets||{},h=matchPhrases(q,groups);if(!h.length)return{id:null,type:'ASSET_CLASS',confidence:0,source:'UNRESOLVED'};const b=h[0];return{id:b.semantic,type:'ASSET_CLASS',confidence:Math.min(.99,.7+b.score/100),source:'LANGUAGE_VOCABULARY',surface:b.alias}}
function parse(question,context={}){const surface=text(question),q=norm(surface),lang=isBn(surface)?'bn':'en',questionType=detectQuestion(q,lang),country=resolveEntity(q,runtime.countries,'COUNTRY'),resource=resolveEntity(q,runtime.resources,'RESOURCE'),asset=inferAsset(q,lang);const contextCountry=text(context.countryId||'').toUpperCase(),contextResource=text(context.resourceId||'').toUpperCase();if(!country.id&&/^[A-Z]{3}$/.test(contextCountry)){country.id=contextCountry;country.confidence=1;country.source='RUNTIME_CONTEXT'}if(!resource.id&&contextResource){resource.id=contextResource;resource.confidence=1;resource.source='RUNTIME_CONTEXT'}const operation=questionType.type==='COUNT'?'COUNT':questionType.type==='QUANTITY'?'QUANTITY':questionType.type==='LOCATION'?'LOCATE':questionType.type==='SELECT'?'SELECT':questionType.type==='CAUSE'?'ANALYZE_CAUSE':questionType.type==='POLICY'?'EVALUATE_POLICY':questionType.type==='FEASIBILITY'?'CHECK_FEASIBILITY':questionType.type==='FORECAST'?'FORECAST':detectOperator(q,lang)||questionType.type;let intent='GENERAL_QUERY';if(asset.id)intent='RESOURCE_MINING_DISCOVERY';else if(resource.id)intent='RESOURCE_QUERY';if(operation==='ANALYZE_CAUSE')intent='CAUSAL_ANALYSIS';if(operation==='EVALUATE_POLICY')intent='POLICY_EVALUATION';if(operation==='LOCATE')intent='LOCATION_QUERY';const unresolved=[];if(asset.id&&!country.id)unresolved.push('COUNTRY');if(asset.id&&!resource.id)unresolved.push('RESOURCE');const executable=unresolved.length===0&&(country.id||resource.id||intent==='GENERAL_QUERY');return Object.freeze({version:VERSION,source:'OFFLINE_SEMANTIC_BRAIN',surface,language:lang,question:questionType,intent,operation,entities:{country,resource,asset},context:{countryId:context.countryId||null,resourceId:context.resourceId||null},unresolved,confidence:Math.min(country.id?country.confidence:1,resource.id?resource.confidence:1,asset.id?asset.confidence:1),executable})}
function indexDatasetForQuery(query){const n=norm(query),hits=[];const walk=(node,path=[])=>{if(!object(node))return;if(Array.isArray(node)){node.forEach((x,i)=>walk(x,path.concat(i)));return}const blob=norm(Object.entries(node).filter(([k,v])=>typeof v==='string').map(([k,v])=>`${k} ${v}`).join(' '));if(blob&&n.split(' ').filter(w=>w.length>2).some(w=>blob.includes(w)))hits.push({path,value:node});for(const[k,x]of Object.entries(node))if(object(x))walk(x,path.concat(k))};runtime.datasets.forEach(d=>walk(d));return hits.slice(0,200)}
function explain(p){return{language:p.language,questionType:p.question.type,intent:p.intent,operation:p.operation,countryId:p.entities.country.id,resourceId:p.entities.resource.id,assetClass:p.entities.asset.id,unresolved:p.unresolved,confidence:p.confidence,executable:p.executable,runtimeCountryCount:runtime.countries.length,runtimeResourceCount:runtime.resources.length}}
const api=Object.freeze({VERSION,configure,parse,explain,learn,recall:learned,indexDatasetForQuery});global.OfflineSemanticBrain=api;if(typeof module!=='undefined'&&module.exports)module.exports=api})(typeof globalThis!=='undefined'?globalThis:window);
